@model IEnumerable<IB_Company.Models.Category>

<div class="container p-3">
	<div class="row pt-4">
		<div class="col-6">
			<h2 class="text-primary"> Category List</h2>
		</div>
		<div class="col-6 text-right" >
			<a asp-controller="Category" asp-action="Create" class="btn btn-primary">
			 Create New Category (Создание новой таблицы)
			</a>
			
		</div>
	</div>
	<br /><br />
	@if (Model.Count() > 0)
	{
		<table class="table table-bordered table-striped" style="width:100%">
			<thead>
				<tr>
					<th>
						Category Name (Имя категории)
					</th>
					<th>
						Display Order (Порядковый номер)
					</th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				@foreach (var obj in Model)
				{
					<tr>
						<td width="50%"> @obj.Name</td>
						<td width="30%"> @obj.DisplayOrder</td>
						<td></td>
					</tr>
				}
			</tbody>
		</table>

	}
	else
	{
		<p>No category exists.</p>
	}
</div>


@* ПРОВЕРЯЮ КОММИТ В ГИТ  *@
@* т.е вот так пишу код и следом фиксирую комментариями  *@